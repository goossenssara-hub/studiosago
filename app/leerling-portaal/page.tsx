import PageShell from "@/components/PageShell";
import LeerlingPortaalClient from "@/components/leerling/LeerlingPortaalClient";

export default function LeerlingPortaalPage() {
  return (
    <PageShell>
      <LeerlingPortaalClient />
    </PageShell>
  );
}